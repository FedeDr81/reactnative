import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  imageContainer: {
    alignItems: 'center',
    marginTop: 40,
    marginBottom: 40,
  },
  logo: {
    width: 250,
    height: 100,
  },
});

export default styles